// Humna Sultan ; CS55 Final Proj. Steve-Con

// GOAL: hold up to 3 past orders that the user made

// declare array (size 3) that can hold  a list of dictionary items as each element
// declare dictionary
// keys = items; values = quantities of items

// FUNCTIONS:
// if checkout button pressed on page, add the list of items that were purchased to the array that holds lists of dictionary values
// update the values in the array everytime the checkout button is pressed - should be most recent checkouts
// initialize the array that holds the past 3 orders to be empty
// display orders in the array

/*
def initializeArray():
    set the values to be empty because nothing has been checked out yet
*/

/* 
// function for when the checkout button is pressed and recent orders needs to be updated
def updateArray():
    move over the elements in the array (2nd item becomes 1st, 3rd becomes 2nd, the latest checkout becomes 3rd)
        makes sure that the first element (oldest checkout) is removed from the list and it is updated
    add the latest checkout (list of dictionary items) as the 3rd element in the array, representing the most recent purchase
    displayOrders() --> update the 
*/

/*
def displayOrders():
    change display of the past orders on the checkout page to be updated with new values
    display appropriate images
*/
